{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "clang++ build active file",
            "command": "/usr/bin/clang++",
            "args": [
                "-std=c++2a",
                "-stdlib=libc++",
                "-O3",
                "-framework",
                "CoreVideo",
                "${workspaceFolder}/bruh.cpp",
                "-o",
                "${workspaceFolder}/out"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}